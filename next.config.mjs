/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'api.microlink.io', 'chrisnguyen.x10host.com', 'public.boxcloud.com', 'app.box.com', 'x15.x10hosting.com'],
    },
};

export default nextConfig;
